#include <stdio.h>

int main()
{
    int a, b;

    scanf("%d %d", &a, &b);

    if (a == b)
    {
        printf("ngecatnya nanti aja, Mun");
    }

    int luas;

    int hasil = (luas / 2) * 500 + (luas / 8) * 350 + ((3 * luas) / 8) * 210;
}